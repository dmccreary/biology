# Cladogram TODO list

The solutions are trivial.  You just walk up the matrix.  We need
to add a more complex example.

Also, fix the layout so some of the end-case labels are not being
overlapped with UI components.  The topmost and last labels
in the central cladogram are overlapped.