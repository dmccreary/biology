# Punnett Square Calculator — TODO

- Narrow iframe widths (<640 px) still cause mild crowding between the left control deck and the grid headers. Adjust responsive breakpoints to stack the panels earlier or scale fonts down further.
- Confirm that the control deck buttons wrap gracefully on small screens; add breakpoint-specific layout tweaks if needed.
